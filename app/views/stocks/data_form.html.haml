%h1
	=link_to root_path do
		%i.glyphicon.glyphicon-home
	=title 'Enter stock details'

%hr

- if @stock.errors.any?
	.alert.alert-danger
		%button.close{:data => {:dismiss => 'alert'}}
			&times;
		- @stock.errors.full_messages.each do |message|
			= message
			%br

.form
	= form_for @stock, :html => {:class => 'form-horizontal'} do |f|
		
		.form-group
			= f.label :stock_name, :class => 'col-lg-4 control-label'
			.col-lg-6
				= f.text_field :stock_name, :class => 'form-control', :placeholder => 'Enter name...', :data => {'toggle' => 'tooltip', 'original-title' => 'Please enter a unique name'}
		
		.form-group
			= f.label :price, :class => 'col-lg-4 control-label'
			.col-lg-6
				= f.text_field :price, :class => 'form-control', :placeholder => 'Enter price...', :data => {'toggle' => 'tooltip', 'original-title' => 'Please enter a price (It can be a decimal)'}
		
		.form-group
			= f.label :quantity, :class => 'col-lg-4 control-label'
			.col-lg-6
				= f.text_field :quantity, :class => 'form-control', :placeholder => 'Enter quantity...', :data => {'toggle' => 'tooltip', 'original-title' => 'Please enter a quantity (It can only be a whole number)'}
		
		.form-group
			= f.label :percentage, :class => 'col-lg-4 control-label'
			.col-lg-6
				= f.text_field :percentage, :class => 'form-control', :placeholder => 'Enter percentage...', :data => {'toggle' => 'tooltip', 'original-title' => 'Please enter a percentage (It can be a decimal)'}

		.form-group
			= f.label :years, :class => 'col-lg-4 control-label'
			.col-lg-6
				= f.text_field :years, :class => 'form-control', :placeholder => 'Enter years...', :data => {'toggle' => 'tooltip', 'original-title' => 'Please enter the years (It can only be a whole number)'}

		.form-group
			.col-lg-offset-4.col-lg-6
				= button_tag(type: 'submit', class: "btn btn-primary") do
					%i.glyphicon.glyphicon-ok-sign
					Calculate